setup:
  addons:
  - plan: heroku-postgresql
build:
  docker:
    web: ./backend/Dockerfile
    frontend: ./backend/frontend/Dockerfile
release:
  image: web
run:
  web: gunicorn config.wsgi && python manage.py collectstatic
  frontend: npm run build && npm start