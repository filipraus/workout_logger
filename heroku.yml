setup:
  addons:
  - plan: heroku-postgresql
build:
  docker:
    backend: ./backend/Dockerfile
    frontend: ./frontend/Dockerfile
release:
  image: backend
  command:
    - python manage.py collectstatic --noinput
run:
  backend: gunicorn config.wsgi